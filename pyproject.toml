[project]
name = "mcp-coalesce"
version = "0.1.0"
dependencies = [
    "mcp[cli]>=1.8",
    "starlette>=0.46",
    "uvicorn>=0.34",
]

[project.scripts]
custom-server = "server:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"